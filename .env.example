# Neon Postgres connection string
DATABASE_URL=postgresql://neondb_owner:PASSWORD@ep-xxxx.us-west-2.aws.neon.tech/neondb?sslmode=require

# Grafana (optional, local only)
GRAFANA_PASSWORD=admin

# Render deployment
# Set DATABASE_URL as an environment variable in the Render dashboard:
# https://dashboard.render.com → Service → Environment → Add DATABASE_URL
